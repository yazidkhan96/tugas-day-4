const Detail = (props) => {
    return (
        <div>
            <div>Detail</div>
            <button onClick={props.tambahKeranjang}>Tambah Keranjang</button>
        </div>
    );
}

export default Detail;